name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.12'
  NODE_VERSION: '18'

jobs:
  lint-and-test:
    name: Lint and Test
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: mukit
          POSTGRES_PASSWORD: mukit
          POSTGRES_DB: mukit_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Backend setup and linting
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Cache Python dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff mypy bandit safety pytest pytest-asyncio httpx pytest-cov

      - name: Run backend linters
        working-directory: ./backend
        run: |
          echo "Running Ruff linter..."
          ruff check app/ --show-fixes || exit 1
          
          echo "Running Ruff formatter check..."
          ruff format app/ --check --diff || exit 1
          
          echo "Running MyPy type checker..."
          mypy app/ --show-error-codes --strict || exit 1
          
          echo "Running Bandit security linter..."
          bandit -r app/ -f json -o bandit-report.json || exit 1
          
          echo "Running pip-audit..."
          pip-audit --format=json --output=pip-audit-report.json || true

      # Frontend setup and linting
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Run frontend linters
        working-directory: ./frontend
        run: |
          echo "Running ESLint..."
          npm run lint || exit 1
          
          echo "Running Prettier check..."
          npm run format:check || exit 1
          
          echo "Running TypeScript type check..."
          npm run type-check || exit 1

      # Backend tests
      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client

      - name: Wait for PostgreSQL to be ready
        run: |
          until pg_isready -h localhost -p 5432 -U mukit; do
            echo "Waiting for PostgreSQL to be ready..."
            sleep 2
          done
          echo "PostgreSQL is ready!"

      - name: Run backend tests
        working-directory: ./backend
        env:
          DATABASE_URL: postgresql+asyncpg://mukit:mukit@localhost:5432/mukit_test
        run: |
          python -m pytest tests/ -v --tb=short || exit 1

      # Frontend tests
      - name: Run frontend tests
        working-directory: ./frontend
        run: |
          npm run test:run || exit 1

  deploy:
    name: Deploy to VPS
    needs: lint-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate required secrets
        run: |
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "❌ Error: SSH_PRIVATE_KEY secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.SSH_HOST }}" ]; then
            echo "❌ Error: SSH_HOST secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.SSH_USER }}" ]; then
            echo "❌ Error: SSH_USER secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.DEPLOY_PATH }}" ]; then
            echo "❌ Error: DEPLOY_PATH secret is not set"
            exit 1
          fi
          echo "✅ All required secrets are set"

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.SSH_PORT || 22 }} ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Verify frontend environment variables
        run: |
          echo "📝 Checking frontend environment variables..."
          if [ -z "${{ secrets.VITE_API_URL }}" ]; then
            echo "❌ Error: VITE_API_URL secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.VITE_WS_URL }}" ]; then
            echo "❌ Error: VITE_WS_URL secret is not set"
            exit 1
          fi
          echo "✅ Frontend environment variables are set"

      - name: Deploy backend to server
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          ALLOWED_ORIGINS: ${{ secrets.ALLOWED_ORIGINS }}
        run: |
          echo "📦 Deploying backend code to server..."
          
          # Create backend directory structure
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "mkdir -p $DEPLOY_PATH/backend"
          
          # Copy backend code to server using tar (more reliable than rsync)
          # Create a tar archive excluding unnecessary files
          tar --exclude='__pycache__' \
              --exclude='*.pyc' \
              --exclude='.pytest_cache' \
              --exclude='.env' \
              --exclude='uploads' \
              --exclude='*.log' \
              --exclude='test.db' \
              --exclude='pip-audit-report.json' \
              --exclude='bandit-report.json' \
              -czf - -C backend . | \
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "cd $DEPLOY_PATH && tar -xzf - -C backend"
          
          # Create uploads directory if it doesn't exist
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "mkdir -p $DEPLOY_PATH/backend/uploads"
          
          echo "✅ Backend code deployed"
          
          # Setup and deploy on server
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST bash << REMOTE_EOF
            set -e
            cd $DEPLOY_PATH
            
            echo "📝 Creating/updating backend .env file..."
            cat > backend/.env << ENVEOF
          DATABASE_URL=$DATABASE_URL
          REDIS_URL=$REDIS_URL
          SECRET_KEY=$SECRET_KEY
          ALLOWED_ORIGINS=$ALLOWED_ORIGINS
          ENVEOF
            
            cd backend 
            echo "🐍 Setting up Python virtual environment..."
            if [ ! -d ".venv" ]; then
              python3.12 -m venv .venv
            fi
            source .venv/bin/activate
            
            echo "📦 Upgrading pip, setuptools, and wheel..."
            pip install --upgrade pip setuptools wheel
            
            echo "📦 Installing Python dependencies..."
            pip install --no-cache-dir --index-url https://pypi.org/simple/ -r requirements.txt || {
              echo "❌ Failed to install dependencies. Trying without explicit index..."
              pip install --no-cache-dir -r requirements.txt
            }
            
            echo "🗄️  Running database migrations..."
            alembic upgrade head
            
            echo "🔄 Restarting backend service..."
            
            echo "⏳ Waiting for services to start..."
            sleep 5
            
            echo "✅ Backend deployment completed!"
          REMOTE_EOF

      - name: Deploy frontend source code to server
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          echo "📦 Deploying frontend source code to server..."
          
          # Create frontend directory structure on server if it doesn't exist
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "mkdir -p '$DEPLOY_PATH/frontend'"
          
          # Verify directory was created
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "test -d '$DEPLOY_PATH/frontend' || { echo 'Failed to create frontend directory'; exit 1; }"
          
          # Copy frontend source code to server using tar (excluding node_modules, dist, etc.)
          echo "Copying frontend source code to server..."
          tar --exclude='node_modules' \
              --exclude='dist' \
              --exclude='.git' \
              --exclude='.vite' \
              --exclude='coverage' \
              --exclude='*.log' \
              -czf - -C frontend . | \
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "cd '$DEPLOY_PATH/frontend' && tar -xzf -"
          
          # Verify copy was successful
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "test -f '$DEPLOY_PATH/frontend/package.json' || { echo 'Failed to copy frontend source'; exit 1; }"
          
          echo "✅ Frontend source code deployed"
          
          # Build and setup frontend on server
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST bash << REMOTE_EOF
            set -e
            cd $DEPLOY_PATH/frontend
            
            # Set up Node.js paths - npm needs node in PATH
            export NODE_PATH="/var/www/uludupec/data/.nvm/versions/node/v24.13.0"
            export PATH="\$NODE_PATH/bin:\$PATH"
            
            echo "📝 Setting up frontend environment variables..."
            export VITE_API_URL="${{ secrets.VITE_API_URL }}"
            export VITE_WS_URL="${{ secrets.VITE_WS_URL }}"
            
            echo "📝 Creating frontend .env file..."
            # Use socket mode - default socket path for this hosting provider
            # You can override this by setting FRONTEND_SOCKET in GitHub Secrets
            if [ -n "${{ secrets.FRONTEND_SOCKET }}" ]; then
              SOCKET_PATH="${{ secrets.FRONTEND_SOCKET }}"
            else
              SOCKET_PATH="/var/www/uludupec/data/nodejs/4.sock"
            fi
            
            # Ensure socket directory exists with proper permissions
            SOCKET_DIR="\$(dirname "\$SOCKET_PATH")"
            mkdir -p "\$SOCKET_DIR"
            chmod 755 "\$SOCKET_DIR" || true
            
            cat > .env << ENVEOF
          VITE_API_URL=${{ secrets.VITE_API_URL }}
          VITE_WS_URL=${{ secrets.VITE_WS_URL }}
          SOCKET=\$SOCKET_PATH
          ENVEOF
            
            echo "📝 Frontend will use socket: \$SOCKET_PATH"
            
            echo "📦 Installing Node.js dependencies..."
            npm ci
            
            echo "✅ Frontend build completed on server"
          REMOTE_EOF
          
          echo "✅ Frontend deployment completed successfully"

      - name: Health check
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PORT: ${{ secrets.SSH_PORT || 22 }}
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
        run: |
          echo "🏥 Performing health check..."
          API_URL=$(echo "${{ secrets.VITE_API_URL }}" | sed 's|/api/v1||')
          ssh -p $SSH_PORT $SSH_USER@$SSH_HOST "curl -f $API_URL/health || echo '⚠️  Health check failed'" || true

